1.0 : host header injection 
		verify domain for each page ie in index.php
		$allowed_domains = array('yourdomain1', 'yourdomain2', 'yourdomain3');

		$default_domain  = 'yourprimedomain';
		if (in_array($_SERVER['HTTP_HOST'], $allowed_domains, TRUE))
		{
		    $domain = $_SERVER['HTTP_HOST'];
		}
		else
		{
		    $domain = $default_domain;
		}

		if (! empty($_SERVER['HTTPS']))
		{
		    $config['base_url'] = 'https://'.$domain;
		}
		else
		{
		    $config['base_url'] = 'http://'.$domain;
		}

2.0  Vulnerability: Broken Authentication
	delete session properly on sever when user logged out


3.0  Vulnerability: Sensitive Data Exposure
		sen dencrypted value instead of palin

4.0  Broken Access Control
	 every page not authenticated and by replacing cookie value page is accessible in new browser

5.0  Security Misconfiguration
	disable show of sever details

6.0  Using Known Vulnerable Components
		disable server details

7.0 Insufficient Logging and Monitoring
	activity, login, history maintain at application level

8.0 Insufficient Logging and Monitoring
		any file can be uplaoded

9.0  Weak Password Policy
		strong passsword can only be saved

10.0  Cross Site Request Forgery (CSRF) 
	 every form submit should ve veried by token   (CSRF Gaurd use)

11.0   Directory Browsing
		disable directory listing of sever folders

12.0   Auto Fill Feature Enabled
		login/registration form auto fill browser feature should be turned off

13.0  Account takeover
		password changed of another user. hide sensitive informations

14.0  Click Jacking
		iframe display should be disabled

15.0 malicious page can be inserted (in profile update page)
	xss protection on 
16.0 chnage password: old password, new password and confirm password must be mendotry










